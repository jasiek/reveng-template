# Genuine ST STM32F1xx. Passive attach (no reset), for a target whose firmware
# remaps the SWD pins after boot — see probe.sh.
source [find interface/stlink.cfg]
transport select hla_swd
reset_config none
adapter speed 1000
source [find target/stm32f1x.cfg]
